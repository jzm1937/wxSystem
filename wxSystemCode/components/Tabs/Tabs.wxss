.tabs_title{
     display: flex;
}

.title_item{
     display: flex;
     justify-content: center;
     padding: 15rpx 0;
     align-items: center;
     flex: 1;
}
.active{
     color: var(--themeColor);
     border-bottom: 5rpx solid currentColor;
}